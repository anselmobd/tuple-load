csv:

  keys:
    - ERRADO

sql:

  db: systextil

  type: script

  script:

    command1: |
      SELECT
        NULL
      FROM HDOC_030
      where :ERRADO != :ERRADO
        AND :CERTO != :CERTO
